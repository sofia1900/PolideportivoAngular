import { Injectable } from '@angular/core';
import {Observable} from "rxjs";

@Injectable({
  providedIn: 'root'
})
export class PartidosService {
  constructor() { }

  getPartidos () {
    return of(Parti)
  }
}
